/// A list of a commands to manipulate the ANSI terminal
pub mod terminal;
/// Screen stuffs
pub mod screen;
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
